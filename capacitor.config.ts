import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hackathon.riskapp',
  appName: 'Localized Risk App',
  webDir: 'out'
};

export default config;
